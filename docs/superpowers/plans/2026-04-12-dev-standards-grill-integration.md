# dev-standards Grill Integration — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fold Karpathy-style discipline and grill-me task interrogation into `dev-standards`, and wire `dartai` and `workflow` to consume them at runtime instead of duplicating playbook content.

**Architecture:** Three layers — (1) thin always-loaded `.claude/rules/*.md` pointers, (2) on-demand `plugins/dev-standards/skills/*` skills carrying operational detail, (3) `dartai`/`workflow` plugin consumers with explicit invocation points. Consumers never duplicate layer-2 content; layer-2 reads layer-1 to branch on project posture; backflow writes enrich project context.

**Tech Stack:** Claude Code plugin markdown (skills, commands, agents, rules). No application code — all work is editing/creating markdown and YAML-frontmatter files inside three plugins under `plugins/`. Verification uses `grep`, `wc`, and `test` in bash. No build step. No test runner.

**Spec reference:** `docs/superpowers/specs/2026-04-12-dev-standards-grill-integration-design.md`.

**Repo layout context:**
- `plugins/dev-standards/` — the playbook plugin (owner of new rules + skills)
- `plugins/dartai/` — Dart-backed task execution (consumer)
- `plugins/workflow/` — generic loop orchestration (consumer)
- `plugins/dev-standards/assets/templates/rules/` — templates that `setup-project` copies into target projects' `.claude/rules/`
- `plugins/dev-standards/assets/templates/skills/{docs,ddd,tdd,webapp,...}/` — skill templates copied into target projects' `.claude/skills/`

**TDD adaptation for markdown:** This plan has no compile/test cycle. Each task follows a modified RED/GREEN where "RED" is a bash verification command that currently fails, "GREEN" is the edit that makes it pass, and "refactor" is a re-run of the verification plus a line-count/style check. Commit after each GREEN.

**Branch discipline:** Work on a dedicated worktree branch off `main`. Do **not** amend commits. Never commit build artifacts or `.superpowers/` mockups (already in `.gitignore` via this plan's Task 0 if absent).

---

## Task 0: Worktree, branch, and gitignore setup

**Files:**
- Modify: `.gitignore` (add `.superpowers/` if absent)

- [ ] **Step 1: Confirm current branch and clean state**

Run: `git status -s && git branch --show-current`
Expected: current branch is `main` or a clean feature branch; no unstaged changes that would be clobbered. If dirty, stash or commit before continuing.

- [ ] **Step 2: Create worktree and branch**

Run: `git worktree add ../standardbeagle-tools-grill-integration -b feat/dev-standards-grill-integration`
Expected: new worktree path printed, branch created off current HEAD.

- [ ] **Step 3: Change into worktree**

Run: `cd ../standardbeagle-tools-grill-integration`
Expected: pwd ends in `standardbeagle-tools-grill-integration`.

All subsequent file paths in this plan are relative to the worktree root.

- [ ] **Step 4: Add .superpowers/ to .gitignore if missing**

Run: `grep -q '^\.superpowers/' .gitignore || echo '.superpowers/' >> .gitignore`
Expected: `.gitignore` now contains the line.

- [ ] **Step 5: Verify**

Run: `grep '^\.superpowers/' .gitignore`
Expected: `.superpowers/` printed.

- [ ] **Step 6: Commit**

```bash
git add .gitignore
git commit -m "chore: ignore .superpowers/ brainstorm mockups"
```

---

# Phase A — dev-standards rule templates

Thin always-loaded rules. Target: each file under 60 lines. Each references skills, embeds no procedure.

## Task 1: Create `karpathy-principles.md` rule template

**Files:**
- Create: `plugins/dev-standards/assets/templates/rules/karpathy-principles.md`

- [ ] **Step 1: Write verification that currently fails**

Run: `test -f plugins/dev-standards/assets/templates/rules/karpathy-principles.md && echo EXISTS || echo MISSING`
Expected: `MISSING`

- [ ] **Step 2: Create the file with this exact content**

````markdown
<!-- Generated by dev-standards plugin. Customize as needed. -->
---
description: Five named development principles distilled from Karpathy's observations on LLM coding pitfalls. Reference only — operational detail lives in dev-standards skills.
---

# Karpathy Development Principles

Five principles govern how development proceeds in this project. Each is intentionally short; the skill listed below each principle carries the detail.

## 1. Goal-Driven Execution

Turn imperative instructions into declarative goals with verification loops. Do not run a checklist blindly; know what "done" looks like before you start.

See `dev-standards:grill-task` for how to extract the goal at intake and `superpowers:verification-before-completion` for verifying success.

## 2. Push Back on Assumptions

If a request is ambiguous, surface the ambiguity before acting. Do not guess when asking is cheap. Do not "run with it" when you are unsure which branch of a decision tree applies.

See `dev-standards:grill-task` for the interrogation pattern.

## 3. Surface Tradeoffs Before Deciding

When two approaches are both defensible, present the tradeoff — do not silently pick one. This applies during planning, not just design.

See `dev-standards:grill-task` (design layer) and `dev-standards:refactor-first-assessment`.

## 4. Verify Before Claiming Success

Evidence precedes assertions. Do not report a task as complete until a verification step has actually run and returned the expected output.

See `superpowers:verification-before-completion`.

## 5. No Stealth Scope Creep

Every change traces to the current task's acceptance criteria. Work discovered during execution becomes a plan update proposal, not a silent edit.

See `dev-standards:refactor-discipline.md` and `dev-standards:review-for-plan-updates`.
````

- [ ] **Step 3: Verify the file exists and is under 60 lines**

Run: `test -f plugins/dev-standards/assets/templates/rules/karpathy-principles.md && wc -l plugins/dev-standards/assets/templates/rules/karpathy-principles.md`
Expected: file exists, line count ≤ 60.

- [ ] **Step 4: Verify it references skills, not procedures**

Run: `grep -c 'dev-standards:\|superpowers:' plugins/dev-standards/assets/templates/rules/karpathy-principles.md`
Expected: ≥ 5 (one per principle, at least).

- [ ] **Step 5: Commit**

```bash
git add plugins/dev-standards/assets/templates/rules/karpathy-principles.md
git commit -m "feat(dev-standards): add karpathy-principles rule template"
```

---

## Task 2: Create `refactor-discipline.md` rule template

**Files:**
- Create: `plugins/dev-standards/assets/templates/rules/refactor-discipline.md`

- [ ] **Step 1: Verification currently fails**

Run: `test -f plugins/dev-standards/assets/templates/rules/refactor-discipline.md && echo EXISTS || echo MISSING`
Expected: `MISSING`

- [ ] **Step 2: Create the file with this exact content**

````markdown
<!-- Generated by dev-standards plugin. Customize as needed. -->
---
description: The A/B/C refactor rule. A (preparatory, in plan) is allowed. B (opportunistic, while-we're-at-it) is forbidden. C (surfaced on review, becomes new task) is allowed. Reference only.
---

# Refactor Discipline

Three classes of refactoring exist. Only two are allowed.

## A. Preparatory refactor — ALLOWED (in plan)

Before implementing a change, assess whether the existing structure will fight it. If it will, insert a preparatory refactor step into the plan *before* the implementation steps. Never skip the pre-refactor and hack the new code into a bad location.

See `dev-standards:refactor-first-assessment` for how to run the check during planning.

## B. Opportunistic refactor — FORBIDDEN

"While I'm already in this file, I may as well clean up adjacent code" is the primary source of scope creep and the primary reason plans drift. Do not do it. Touch only what the task requires.

If you notice something worth changing, record it as a review finding (see C) and move on.

## C. Review-discovered refactor — ALLOWED (as plan update)

After a task reaches GREEN, the review phase may surface C-class findings: parameter bloat, cross-task duplication, naming drift, grown responsibilities, test setup duplication, visible complexity debt, dead branches. These do NOT modify the just-finished task. They become structured **plan update proposals** that the planner decides to accept, defer, or reject.

See `dev-standards:review-for-plan-updates` for the trigger catalog, proposal format, and routing.

## Backlog configuration

Plan-update proposals carry an urgency (`low` / `medium` / `high`). `low` proposals auto-expire after 30 days to prevent unbounded backlog growth. `medium` and `high` proposals do not auto-expire. Override the expiry window by editing this file — the review skill reads it at runtime.

```yaml
backlog:
  low_urgency_expiry_days: 30
```
````

- [ ] **Step 3: Verify file exists and line count**

Run: `test -f plugins/dev-standards/assets/templates/rules/refactor-discipline.md && wc -l plugins/dev-standards/assets/templates/rules/refactor-discipline.md`
Expected: exists, line count ≤ 60.

- [ ] **Step 4: Verify it names A/B/C explicitly and points to the two skills**

Run: `grep -E 'A\.|B\.|C\.' plugins/dev-standards/assets/templates/rules/refactor-discipline.md | wc -l && grep -c 'refactor-first-assessment\|review-for-plan-updates' plugins/dev-standards/assets/templates/rules/refactor-discipline.md`
Expected: ≥ 3 A/B/C marks; ≥ 2 skill references.

- [ ] **Step 5: Commit**

```bash
git add plugins/dev-standards/assets/templates/rules/refactor-discipline.md
git commit -m "feat(dev-standards): add refactor-discipline rule template"
```

---

## Task 3: Create `grill-intake.md` rule template

**Files:**
- Create: `plugins/dev-standards/assets/templates/rules/grill-intake.md`

- [ ] **Step 1: Verification currently fails**

Run: `test -f plugins/dev-standards/assets/templates/rules/grill-intake.md && echo EXISTS || echo MISSING`
Expected: `MISSING`

- [ ] **Step 2: Create the file with this exact content**

````markdown
<!-- Generated by dev-standards plugin. Customize as needed. -->
---
description: Tier-gated task intake interrogation rule. When any plugin creates a task or schedules work, it invokes dev-standards:grill-task. Reference only — the question bank and procedure live in the skill.
---

# Grill Intake Rule

"Measure twice, cut once" is baked in at task creation, not discovered mid-execution.

## Trigger

Any time a task is being created (via `/dartai:task`, `/workflow:add-task`, or any planning skill emitting a task spec), invoke `dev-standards:grill-task` before the task is persisted.

## Tier gate

The grill classifies the request into one of four tiers (minimal, standard, comprehensive, architectural) and decides depth accordingly.

- **Minimal** — single file, trivial, unambiguous: skip the grill, return a one-line spec.
- **Standard / Comprehensive / Architectural** — run the full layered interrogation, with depth scaling to tier.

## Output channels

Grill-task writes to two channels:

1. **Task spec (ephemeral)** — scope, acceptance, references to project files, not embedded content.
2. **Project context (permanent)** — discovered gaps invoke writer skills (`decide`, `domain-update`, `write-user-story`, etc.) so the next task's grill benefits from enrichment.

See `dev-standards:grill-task` for the six question layers, skip discipline, hard caps (max 12 questions, max 2 follow-up rounds), and the graceful degradation rule when project-local writer skills are absent.
````

- [ ] **Step 3: Verify file exists and line count**

Run: `test -f plugins/dev-standards/assets/templates/rules/grill-intake.md && wc -l plugins/dev-standards/assets/templates/rules/grill-intake.md`
Expected: exists, line count ≤ 60.

- [ ] **Step 4: Verify it points to the skill without embedding its content**

Run: `grep -c 'grill-task' plugins/dev-standards/assets/templates/rules/grill-intake.md`
Expected: ≥ 3.

Also run: `grep -c '^[0-9]\.' plugins/dev-standards/assets/templates/rules/grill-intake.md`
Expected: small number (we are not embedding a numbered procedure).

- [ ] **Step 5: Commit**

```bash
git add plugins/dev-standards/assets/templates/rules/grill-intake.md
git commit -m "feat(dev-standards): add grill-intake rule template"
```

---

## Task 4: Wire new rule templates into `setup-project`

**Files:**
- Modify: `plugins/dev-standards/commands/setup-project.md` (Phase 5, Step 2)

Today, Phase 5 Step 2 copies `version-control.md` and `code-quality.md` as always-loaded rules. We add the three new rule templates to the same step.

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'karpathy-principles\|refactor-discipline\|grill-intake' plugins/dev-standards/commands/setup-project.md`
Expected: `0`

- [ ] **Step 2: Open `plugins/dev-standards/commands/setup-project.md` and locate Step 2 (around line 219-226)**

The current block reads:

```markdown
### Step 2: Copy Always-Loaded Rules

Read and copy these templates to `.claude/rules/`:

- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/version-control.md` -> `.claude/rules/version-control.md`
- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/code-quality.md` -> `.claude/rules/code-quality.md`

Copy them as-is. They have no placeholders that need filling.
```

- [ ] **Step 3: Replace that block with the following**

```markdown
### Step 2: Copy Always-Loaded Rules

Read and copy these templates to `.claude/rules/`:

- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/version-control.md` -> `.claude/rules/version-control.md`
- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/code-quality.md` -> `.claude/rules/code-quality.md`
- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/karpathy-principles.md` -> `.claude/rules/karpathy-principles.md`
- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/refactor-discipline.md` -> `.claude/rules/refactor-discipline.md`
- `${CLAUDE_PLUGIN_ROOT}/assets/templates/rules/grill-intake.md` -> `.claude/rules/grill-intake.md`

Copy them as-is. They have no placeholders that need filling.
```

- [ ] **Step 4: Verify the edit landed**

Run: `grep -c 'karpathy-principles\|refactor-discipline\|grill-intake' plugins/dev-standards/commands/setup-project.md`
Expected: `3`

- [ ] **Step 5: Commit**

```bash
git add plugins/dev-standards/commands/setup-project.md
git commit -m "feat(dev-standards): copy karpathy/refactor/grill rules in setup-project"
```

---

# Phase B — dev-standards operational skills

Heavy-lift content moves into skills. Each skill reads project rules to branch on posture.

## Task 5: Create `refactor-first-assessment` skill

This extracts `dartai/skills/simple-planning.md` Step 2.5 into a standalone skill so `workflow` can reuse it.

**Files:**
- Create: `plugins/dev-standards/skills/refactor-first-assessment/SKILL.md`

- [ ] **Step 1: Verification currently fails**

Run: `test -f plugins/dev-standards/skills/refactor-first-assessment/SKILL.md && echo EXISTS || echo MISSING`
Expected: `MISSING`

- [ ] **Step 2: Create the directory and file with this content**

```bash
mkdir -p plugins/dev-standards/skills/refactor-first-assessment
```

Then write `plugins/dev-standards/skills/refactor-first-assessment/SKILL.md`:

````markdown
---
name: refactor-first-assessment
description: The A-rule preparatory refactor check. Runs during planning, after grill-task, before implementation steps are written. Decides whether to insert a preparatory refactor step or sign off on the current structure.
---

# Refactor-First Assessment

Assess whether the existing codebase structure supports the planned change naturally. If it does not, insert a preparatory refactor step into the plan *before* implementation.

## When to run

- During planning, **after** `dev-standards:grill-task` has produced a task spec
- **Before** the plan's implementation steps are written
- Once per task, not once per file

## Inputs

- Task spec from grill-task (scope, intent, domain terms)
- Current file contents of files in `scope.files_to_modify`
- LCI search results for the task's key nouns/verbs (find the natural home)

## Four checks

Run these in order. Any "no" adds a preparatory refactor step to the plan.

### 1. Natural extension

Is there an obvious place to add this change? Can you point to a file and line where the new code clearly belongs?

- **Yes:** move on.
- **No:** the structure needs a home created first. Add a refactor step: "create the home for X by extracting Y from Z."

### 2. Naming fit

Do existing names make room for the new concept, or would the new code overlap with a differently-named existing concept?

- **Yes:** move on.
- **No:** add a refactor step to rename the existing concept or clarify the naming before the new code lands.

### 3. Co-location

Would related code end up in the right file/module? Specifically, does the new code's domain concept already live somewhere, or is it being scattered across unrelated files?

- **Yes:** move on.
- **No:** add a refactor step to consolidate the domain's existing code before adding more to it.

### 4. Friction

Would you need to fight existing code to add the new change? Signs: deep nesting, unrelated parameters threaded through, callers updated in places that should not care.

- **No (no friction):** move on.
- **Yes (friction):** add a refactor step to reduce the friction first.

## Output

A plan-edit instruction, one of:

- **Sign off:** "No refactor needed. Proceed with implementation as planned."
- **Insert refactor step:** "Before implementation, add step: `Refactor <target> to <action> (move/rename/extract/inline)`. Then re-run existing tests before the first new RED test."

If multiple checks failed, insert multiple refactor steps in the order: extract/move → rename → reduce friction. Never combine refactor and implementation in the same step.

## Discipline

- Refactor steps must pass existing tests before the new RED test is written. If the refactor breaks an existing test, that is a bug — fix it as part of the refactor step, not the implementation step.
- Refactor steps are **A-class only** (preparatory, in plan). They are not a backdoor for opportunistic cleanup. See `.claude/rules/refactor-discipline.md`.
- If more than two refactor steps are needed, the task is likely too large — escalate to "split this task" rather than pile on refactors.

## Example output

```yaml
assessment:
  natural_extension: yes
  naming_fit: no        # "OrderLine" and "LineItem" both in use
  co_location: yes
  friction: no

refactor_steps_to_insert:
  - "Rename OrderLine -> LineItem across src/orders/ and tests/orders/"
  - "Run existing test suite, confirm GREEN"

then_proceed_with:
  - "Original implementation steps from plan"
```
````

- [ ] **Step 3: Verify skill file exists and has valid frontmatter**

Run: `test -f plugins/dev-standards/skills/refactor-first-assessment/SKILL.md && head -5 plugins/dev-standards/skills/refactor-first-assessment/SKILL.md`
Expected: file exists; first lines show `---`, `name: refactor-first-assessment`, `description: ...`, `---`.

- [ ] **Step 4: Verify it names all four checks**

Run: `grep -c '^### [1-4]\.' plugins/dev-standards/skills/refactor-first-assessment/SKILL.md`
Expected: `4`

- [ ] **Step 5: Commit**

```bash
git add plugins/dev-standards/skills/refactor-first-assessment/
git commit -m "feat(dev-standards): add refactor-first-assessment skill"
```

---

## Task 6: Create `grill-task` skill

The intake interrogation skill. Six question layers with skip discipline, tier gate, hard caps, two output channels, graceful degradation for absent project-local writers.

**Files:**
- Create: `plugins/dev-standards/skills/grill-task/SKILL.md`

- [ ] **Step 1: Verification currently fails**

Run: `test -f plugins/dev-standards/skills/grill-task/SKILL.md && echo EXISTS || echo MISSING`
Expected: `MISSING`

- [ ] **Step 2: Create directory and file**

```bash
mkdir -p plugins/dev-standards/skills/grill-task
```

Write `plugins/dev-standards/skills/grill-task/SKILL.md`:

````markdown
---
name: grill-task
description: Interrogate a task request before it is persisted, extracting intent, user flow, domain terms, design patterns, scope, and verification. Two output channels — task spec and project-context backflow. Tier-gated; minimal tasks skip entirely.
---

# Grill-Task

Runs at task creation. Measures twice so execution only has to cut once.

## Invocation

```
Called by:
  dartai: /dartai:task, skills/simple-planning, skills/adversarial-planning-loop
  workflow: /workflow:add-task
Returns:
  task_spec (required)
  backflow_writes (list of {skill, path, changes}; may be empty)
```

## Step 0 — Read project context (silent)

Read before asking anything. The more context you have, the fewer questions you need.

- `.claude/rules/*` — project posture (security, testing, TDD level, architecture style)
- `CLAUDE.md` or `.claude/CLAUDE.md` — project identity
- `docs/DOMAIN.md` or `docs/domain/*.md` — canonical domain terms
- `docs/user-stories/` — existing stories
- `docs/user-flows/` — existing flows
- `docs/design-guidelines.md` and any component library READMEs detected
- Recent related tasks (for dartai: same folder; for workflow: `.workflow/loop-state.json.tasks[]`)
- LCI search results for the nouns and verbs in the raw request

## Step 1 — Classify tier

Decide which tier this request falls into:

| Tier | Indicators | Action |
|---|---|---|
| Minimal | 1 file, trivial, unambiguous (typo, text, one-line fix) | **Skip grill entirely**, return one-line spec, done |
| Standard | 2-5 files, single feature/fix, minor clarification needed | Run full layered interrogation |
| Comprehensive | 5+ files, multiple criteria, significant discussion | Run full layered interrogation, all layers |
| Architectural | cross-cutting, multiple subsystems, new patterns | Run full layered interrogation, plus escalation if too large |

For minimal tier, return immediately:

```yaml
task_spec:
  requested: "<user's exact words>"
  tier: minimal
  scope:
    files_to_modify: [<single file>]
  acceptance: [<one-line criterion>]
backflow_writes: []
```

## Step 2 — Layered questions (standard tier and above)

Ask one question per turn, terminal-side, in this order. Skip a layer entirely if its "reads from" already answers it — summarize what you know and ask "is this still accurate?" rather than starting from scratch.

### Layer 1 — Intent

- **Reads from:** `docs/user-stories/`
- **If missing, asks:** Who benefits? What specific outcome? How is success measured?
- **Writes back via:** project-local `write-user-story` (if present)

### Layer 2 — Flow

- **Reads from:** `docs/user-flows/`
- **If missing, asks:** Entry point? What adjacent steps? What exit criteria?
- **Writes back via:** project-local `define-user-flow` (if present)

### Layer 3 — Domain

- **Reads from:** `docs/DOMAIN.md`
- **If missing, asks:** What is the canonical term for this concept? Are there synonyms to avoid?
- **Writes back via:** project-local `domain-update` (if present), or `domain-init` if no DOMAIN.md exists yet

### Layer 4 — Design

- **Reads from:** `docs/design-guidelines.md`, component library README, existing patterns via LCI
- **If missing, asks:** What pattern should this follow? Is there an existing component to reuse?
- **Writes back via:** plugin-local `dev-standards:update-rules` (edits a project rule file)

### Layer 5 — Scope

- **Reads from:** LCI search on nouns/verbs
- **If missing, asks:** Which files need to change? What is explicitly NOT included?
- **Writes back:** task spec only (no permanent writeback)

### Layer 6 — Verification

- **Reads from:** `.claude/rules/testing.md`, `.claude/rules/tdd.md`
- **If missing, asks:** How is done measured? Is there a RED test planned? Any manual verification step?
- **Writes back:** task spec only (no permanent writeback)

## Step 3 — Graceful degradation for absent writers

Before invoking any **project-local** writer skill, probe for its presence:

```bash
test -f .claude/skills/<skill-name>/SKILL.md
```

If absent, do NOT fail the grill. Record a TODO line in the task spec:

```yaml
notes:
  - "TODO: install .claude/skills/write-user-story to persist Intent layer gap"
```

The next iteration of grill-task picks up the writer if the project eventually installs it (via `dev-standards:add-skill` or a later `setup-project` run with a different docs level).

**Plugin-local writers** (`decide`, `update-rules`, `update-project`, `add-skill`) are always available — no probing required.

## Step 4 — Hard caps

- Max **2 rounds** of follow-up questions after the initial pass
- Max **12 questions total** even for architectural tier
- If caps are hit and the request is still ambiguous, escalate: return `task_spec.verdict = "TOO_LARGE_TO_GRILL"` and recommend splitting the task

## Step 5 — Confirmation screen

Before writing either output channel, present both the proposed task spec and all pending backflow writes as a single confirmation screen:

```
Grilled Task Spec
=================
<task spec YAML>

Pending backflow writes
=======================
- write-user-story: docs/user-stories/US-43.md  (new, 42 lines)
- domain-update:   docs/DOMAIN.md  (append LineItem definition)

Approve [y] / Edit field [e] / Another round [r] / Abort [x] ?
```

On `y`: perform all backflow writes atomically (collect as a staging object, apply together), then return the task spec to the caller.
On `e`: ask which field to edit.
On `r`: go to Step 2 with an additional question budget (still under cap).
On `x`: write nothing, return `task_spec.verdict = "ABORTED"`.

## Output format

```yaml
task_spec:
  requested: "<user's exact words>"
  tier: standard | comprehensive | architectural
  refs:
    user_story: docs/user-stories/US-42.md    # or null
    flow: docs/user-flows/checkout.md         # or null
    domain: [Order, LineItem]
    design: rules/design-guidelines.md#buttons
  scope:
    files_to_modify: [<paths, max 5>]
    not_included: [<explicit exclusions>]
  acceptance:
    - <RED→GREEN criterion 1>
    - <RED→GREEN criterion 2>
  verification:
    runner: vitest                             # or pytest, go test, etc.
    manual: <optional manual check>
  notes: []                                    # populated on graceful degradation
  verdict: OK | TOO_LARGE_TO_GRILL | ABORTED

backflow_writes:
  - skill: <skill-name>
    location: plugin-local | project-local
    path: <file path written>
    summary: <one line>
```

## Discipline

- Ask one question per turn. Never stack multiple questions in one message.
- Prefer multiple-choice when possible; open-ended only when no sensible choices exist.
- Never grill a minimal-tier task. Grilling a typo fix is worse than shipping it wrong.
- Never embed project-local writer skill content into this skill. Invoke them.
- Never persist partial backflow writes — collect, confirm, commit atomically.
- Respect `.claude/rules/karpathy-principles.md`: push back when unsure, surface tradeoffs, do not "run with" an assumption.

## Related skills

- `dev-standards:refactor-first-assessment` — invoked by planning skills after grill-task
- `dev-standards:decide` — backflow writer for undecided architecture questions
- `dev-standards:update-rules` — backflow writer for new project rules
- `dev-standards:add-skill` — backflow writer for new project-specific skills
- `dev-standards:update-project` — backflow writer for project description changes
````

- [ ] **Step 3: Verify file exists and has valid frontmatter**

Run: `test -f plugins/dev-standards/skills/grill-task/SKILL.md && head -5 plugins/dev-standards/skills/grill-task/SKILL.md`
Expected: file exists; frontmatter with `name: grill-task` and a `description` field.

- [ ] **Step 4: Verify all six layers are documented**

Run: `grep -c '^### Layer [1-6]' plugins/dev-standards/skills/grill-task/SKILL.md`
Expected: `6`

- [ ] **Step 5: Verify graceful degradation is present**

Run: `grep -c 'TODO: install' plugins/dev-standards/skills/grill-task/SKILL.md`
Expected: ≥ 1

- [ ] **Step 6: Verify hard caps are stated**

Run: `grep -c '2 rounds\|12 questions' plugins/dev-standards/skills/grill-task/SKILL.md`
Expected: ≥ 2

- [ ] **Step 7: Commit**

```bash
git add plugins/dev-standards/skills/grill-task/
git commit -m "feat(dev-standards): add grill-task intake interrogation skill"
```

---

## Task 7: Create `review-for-plan-updates` skill

The post-GREEN reviewer that emits C-class refactor findings as structured plan-update proposals. Does not edit code.

**Files:**
- Create: `plugins/dev-standards/skills/review-for-plan-updates/SKILL.md`

- [ ] **Step 1: Verification currently fails**

Run: `test -f plugins/dev-standards/skills/review-for-plan-updates/SKILL.md && echo EXISTS || echo MISSING`
Expected: `MISSING`

- [ ] **Step 2: Create directory and file**

```bash
mkdir -p plugins/dev-standards/skills/review-for-plan-updates
```

Write `plugins/dev-standards/skills/review-for-plan-updates/SKILL.md`:

````markdown
---
name: review-for-plan-updates
description: Post-GREEN C-class refactor review. Runs after the task's last test passes and before commit. Emits structured plan-update proposals (never edits code). Proposals flow to the calling plugin's planner for accept/defer/reject.
---

# Review for Plan Updates

Look at the diff of a just-completed task and surface C-class refactor discoveries. Never modify the task. Never edit code. Emit structured proposals that the planner can decide about with full release-cadence context.

## When to run

- After the task's last GREEN test passes
- Before the quality gate commits
- Scope-locked — findings never modify the just-finished task

## Inputs

- `git diff <task-base>..HEAD` — only files the task touched
- LCI call graph one hop out from changed symbols (see who is affected)
- `.claude/rules/karpathy-principles.md`
- `.claude/rules/refactor-discipline.md` (urgency thresholds, backlog config)
- `.claude/rules/code-quality.md` (project-specific complexity limits)

## Trigger catalog (C-class only)

Look for these patterns. Each example shows a signal and what a finding for that signal says.

| Signal | Example finding |
|---|---|
| Parameter bloat | "`createOrder` now has 9 params + 3 overloads — options object or builder" |
| Cross-task duplication | "Same validation pattern now written 3× across `checkout/`, `cart/`, `admin/`" |
| Naming drift | "`User`, `Account`, `Customer` all used for same concept — pick one in DOMAIN.md" |
| Grown responsibilities | "`OrderService` crossed 400 lines across last 3 tasks — split candidate" |
| Test setup duplication | "Same fixture built inline in 5 tests — extract builder" |
| Visible complexity debt | "Nested conditional now 4-deep in `applyDiscount` — guard-clause or state machine" |
| Dead branches | "Feature-flag branches exist for a flag that has been on everywhere for 30+ days" |

**Not looked for:** style preferences, unused imports, formatting, anything the linter handles.

## Detection thresholds (read from `.claude/rules/code-quality.md`)

Thresholds are project-specific. Defaults when the rule file does not override:

- Parameters: flag at 5+
- Method lines: flag at 30+
- File lines: flag at 400+
- Nesting depth: flag at 4+
- Duplication: flag at 3+ copies across files

Read the project's `code-quality.md` for overrides before applying.

## Output format

Emit a YAML document with a `proposals` list. Each proposal is structured data, not prose.

```yaml
proposals:
  - title: "Extract OrderCreation options object"
    trigger: parameter_bloat
    evidence:
      symbol: "src/orders/create.ts:createOrder"
      observation: "9 params, 3 overloads, callers pass flags positionally"
      callers: 14   # from LCI
    rationale: "Options object removes positional confusion"
    estimated_tier: standard
    blocks: []
    urgency: low            # low | medium | high
    principle: refactor-discipline.C
```

Proposals reference evidence; they do not embed code.

## Deferred decisions (for the planner, not the reviewer)

The reviewer does **not** decide. Decisions belong to the calling plugin's planner, which sees release cadence, what else is in flight, and team priorities.

- **Accept now** — proposal is grilled as a normal task and scheduled next
- **Defer** — stays in backlog with urgency tag; promoted over time
- **Reject** — marked rejected with a one-line reason; fingerprint recorded to prevent re-proposing

## Reject-list discipline

Before emitting a proposal, compute a fingerprint from (`evidence.symbol`, `trigger`) and check it against the reject list at `.claude/refactor-rejects.txt`. If present, skip emission. The reject list is written by the planner when it rejects a proposal and lives in the project, not the plugin.

## Routing (plugin-specific wrappers handle this)

- **dartai** — proposals become Dart tasks in a `refactor-backlog` folder, tagged `origin:review`, with a link back to the task that surfaced them.
- **workflow** — proposals append to `.workflow/loop-state.json` under `pending_plan_updates[]`.

This skill returns the YAML; the wrapper persists it.

## Discipline

- **Never edit code.** The just-finished task is scope-locked. Any edit here violates refactor-discipline.B.
- **Never decide.** You only see the diff. Accept/defer/reject requires wider context.
- **Evidence, not code.** Proposals reference symbols and file:line locations. The planner reads the files if needed.
- **Short proposals.** Each proposal under 20 lines of YAML. If the rationale needs more, the task is ambiguous — refine the signal instead.

## Related skills

- `dev-standards:grill-task` — proposals that are accepted get grilled as new tasks
- `dev-standards:decide` — if a proposal surfaces an architecture decision, invoke decide when accepted
````

- [ ] **Step 3: Verify file exists and has valid frontmatter**

Run: `test -f plugins/dev-standards/skills/review-for-plan-updates/SKILL.md && head -5 plugins/dev-standards/skills/review-for-plan-updates/SKILL.md`
Expected: file exists; frontmatter shows `name: review-for-plan-updates`.

- [ ] **Step 4: Verify trigger catalog has 7 entries**

Run: `awk '/^\| Signal /{f=1;next} f && /^\| [A-Z]/{count++} /^$/{f=0} END{print count}' plugins/dev-standards/skills/review-for-plan-updates/SKILL.md`
Expected: `7`

- [ ] **Step 5: Verify no decision-making by reviewer**

Run: `grep -c 'never decide\|does not decide\|Never decide' plugins/dev-standards/skills/review-for-plan-updates/SKILL.md`
Expected: ≥ 2

- [ ] **Step 6: Commit**

```bash
git add plugins/dev-standards/skills/review-for-plan-updates/
git commit -m "feat(dev-standards): add review-for-plan-updates refactor review skill"
```

---

# Phase C — dartai integration

Consumers wire explicit invocation points. No operational content duplicated.

## Task 8: Collapse Step 2.5 of `dartai/skills/simple-planning.md`

The inline refactor-first check in Step 2.5 gets replaced with a pointer to the new `dev-standards:refactor-first-assessment` skill, and grill-task is invoked after tier classification.

**Files:**
- Modify: `plugins/dartai/skills/simple-planning.md`

- [ ] **Step 1: Verification currently fails (new references not yet present)**

Run: `grep -c 'dev-standards:grill-task\|dev-standards:refactor-first-assessment' plugins/dartai/skills/simple-planning.md`
Expected: `0`

- [ ] **Step 2: Locate `### Step 2.5: Refactor-First Assessment (1 minute)` (around line 362-389)**

The current block is a ~28-line YAML dictionary describing the refactor-first check inline.

- [ ] **Step 3: Replace the entire `### Step 2.5` block with this shorter pointer**

```markdown
### Step 2.5: Refactor-First Assessment (1 minute)

Invoke `dev-standards:refactor-first-assessment`. It consumes the grilled task spec and decides whether to insert preparatory refactor steps before implementation. The four checks (natural extension, naming fit, co-location, friction) live in that skill — do not duplicate them here.

If the skill returns "sign off", proceed to Step 3. If it returns one or more refactor steps, insert them into the plan before Step 4 (List Steps), in the order: extract/move → rename → reduce friction.
```

- [ ] **Step 4: Insert new Step 0.7 after Step 0.5 (tier classification) and before Step 1 (Capture Request)**

Locate `### Step 1: Capture Request (30 seconds)` (around line 335). Immediately before it, insert:

```markdown
### Step 0.7: Invoke grill-task (after tier classification)

If the tier from Step 0.5 is `minimal`, skip this step — the minimal-tier gate in `dev-standards:grill-task` would skip anyway.

Otherwise, invoke `dev-standards:grill-task` with the raw request and the calling context. The skill returns a `task_spec` and any `backflow_writes`. The `task_spec` replaces steps that follow (Steps 1, 2, and 3 are folded into the grilled spec). Proceed directly to Step 2.5 (Refactor-First Assessment) once grill-task returns.

If grill-task returns `verdict: TOO_LARGE_TO_GRILL`, escalate immediately: report to the user that the task must be split, do not continue planning.
```

- [ ] **Step 5: Verify the references landed**

Run: `grep -c 'dev-standards:grill-task\|dev-standards:refactor-first-assessment' plugins/dartai/skills/simple-planning.md`
Expected: ≥ 2 (one for each skill).

- [ ] **Step 6: Verify Step 2.5 shrunk**

Run: `awk '/^### Step 2.5/{f=1;next} f && /^### /{exit} {if(f)c++} END{print c}' plugins/dartai/skills/simple-planning.md`
Expected: ≤ 10 (was ~28).

- [ ] **Step 7: Commit**

```bash
git add plugins/dartai/skills/simple-planning.md
git commit -m "refactor(dartai): delegate refactor-first and grill to dev-standards skills"
```

---

## Task 9: Wire grill-task into `dartai/commands/task.md`

**Files:**
- Modify: `plugins/dartai/commands/task.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'dev-standards:grill-task' plugins/dartai/commands/task.md`
Expected: `0`

- [ ] **Step 2: Locate `### 2. Confirm Task` (around line 35-43)**

Immediately after the "Confirm Task" section and before "### 3. Execute Pipeline", insert:

```markdown
### 2.5. Grill Task Spec

Before handing off to the task-executor agent, invoke `dev-standards:grill-task` with the task's raw description. The skill probes project context, runs tier-gated interrogation, and returns a grilled task spec.

- Pass the `task_spec` to the task-executor agent in place of the raw description.
- Any `backflow_writes` from the grill are committed to the project before execution starts.
- If grill returns `verdict: TOO_LARGE_TO_GRILL`, update the Dart task status to `Blocked` with a comment recommending the split, and STOP — do not dispatch the task-executor.

Do NOT pre-classify tier here. Let grill-task's own tier classification decide. Minimal-tier tasks pass through grill-task unchanged, so there is no cost to routing everything through it.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'dev-standards:grill-task' plugins/dartai/commands/task.md`
Expected: ≥ 1

- [ ] **Step 4: Commit**

```bash
git add plugins/dartai/commands/task.md
git commit -m "feat(dartai): invoke dev-standards:grill-task in /dartai:task"
```

---

## Task 10: Add grill checkpoint to `dartai/skills/adversarial-planning-loop.md`

For comprehensive and architectural tiers, the adversarial planning loop should confirm grill-task ran and accept the grilled spec as its planning input.

**Files:**
- Modify: `plugins/dartai/skills/adversarial-planning-loop.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'dev-standards:grill-task' plugins/dartai/skills/adversarial-planning-loop.md`
Expected: `0`

- [ ] **Step 2: Read the existing file to find the planning-triangle section near the top**

Run: `grep -n '^### Planning Triangle\|^### Plan Hierarchy' plugins/dartai/skills/adversarial-planning-loop.md`

Note the line number of the first heading that describes planning prose that can be shrunk to a pointer. If that section is under 30 lines and directly references principles already in `karpathy-principles.md`, replace it with a pointer:

```markdown
### Planning Principles (referenced, not embedded)

This skill follows the principles in `.claude/rules/karpathy-principles.md` and the A/B/C refactor rule in `.claude/rules/refactor-discipline.md`. Do not embed their content here; read the rule files at run time.
```

If the planning-triangle content is load-bearing and longer than 30 lines, leave it in place — the goal is not aggressive shrinkage, it is preventing duplication of rule content.

- [ ] **Step 3: Add an explicit grill checkpoint**

Locate the section where planning begins for comprehensive/architectural tier. Add immediately after it:

```markdown
### Grill Checkpoint (comprehensive/architectural tier only)

For comprehensive and architectural tier tasks, verify `dev-standards:grill-task` has produced a task spec before planning proceeds. If no grilled spec is in the caller's context:

1. Invoke `dev-standards:grill-task` with the raw request.
2. Commit any backflow writes returned.
3. Use the returned `task_spec` as the planning input.
4. If the grill returns `TOO_LARGE_TO_GRILL`, stop planning and escalate the split to the user.

Minimal and standard tier tasks do not need this checkpoint — they pass through grill-task naturally via `simple-planning.md` Step 0.7.
```

- [ ] **Step 4: Verify**

Run: `grep -c 'dev-standards:grill-task' plugins/dartai/skills/adversarial-planning-loop.md`
Expected: ≥ 1

Also verify the rule reference landed: `grep -c 'karpathy-principles\|refactor-discipline' plugins/dartai/skills/adversarial-planning-loop.md`
Expected: ≥ 1

- [ ] **Step 5: Commit**

```bash
git add plugins/dartai/skills/adversarial-planning-loop.md
git commit -m "feat(dartai): add grill checkpoint to adversarial-planning-loop"
```

---

## Task 11: Add post-GREEN review step to `dartai/skills/adversarial-quality-loop.md`

**Files:**
- Modify: `plugins/dartai/skills/adversarial-quality-loop.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/adversarial-quality-loop.md`
Expected: `0`

- [ ] **Step 2: Locate the Phase 4 "Quality Gate Verification" section (around line 760)**

Immediately before `## Phase 5: Post-Task Deep Review` (around line 827), insert a new phase:

```markdown
---

## Phase 4.5: Review for Plan Updates (C-class refactor discovery)

### Task: Invoke `dev-standards:review-for-plan-updates`

After quality gates pass but before the deep post-task review, run the C-class refactor review. This emits structured plan-update proposals for things like parameter bloat, duplication, naming drift — without editing any code.

**DO (Positive Instructions):**
- Invoke `dev-standards:review-for-plan-updates` with `git diff <task-base>..HEAD`
- Persist returned proposals as Dart tasks in the `refactor-backlog` folder, tagged `origin:review` with link back to the surfacing task (this persistence is the dartai wrapper responsibility; `dev-standards:code-quality` — now thin — handles it)
- Check each proposal against `.claude/refactor-rejects.txt` fingerprints before persisting

**DO NOT:**
- Edit code based on findings — the current task is scope-locked
- Decide accept/defer/reject yourself — that is the planner's job at the next planning cycle
- Expand the trigger catalog beyond what the skill defines
- Run this phase on tasks that failed quality gates (they will re-run)

**Verification Criteria:**
```yaml
pass_if:
  - review_skill_invoked: true
  - proposals_persisted_or_empty: true
  - no_code_modifications: true
fail_if:
  - any_code_edits_made: true
  - reviewer_decided_accept_reject: true
```
```

- [ ] **Step 3: Verify**

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/adversarial-quality-loop.md`
Expected: ≥ 1

Run: `grep -c '^## Phase 4.5' plugins/dartai/skills/adversarial-quality-loop.md`
Expected: `1`

- [ ] **Step 4: Commit**

```bash
git add plugins/dartai/skills/adversarial-quality-loop.md
git commit -m "feat(dartai): add Phase 4.5 review-for-plan-updates to quality loop"
```

---

## Task 12: Collapse `dartai/skills/code-quality.md` into thin wrapper

Today's file carries a full code-quality playbook duplicating `karpathy-principles.md` and bespoke review content. Collapse to a thin persistence wrapper that invokes `dev-standards:review-for-plan-updates` and routes proposals into Dart.

**Files:**
- Modify: `plugins/dartai/skills/code-quality.md`

- [ ] **Step 1: Verification — count current line length**

Run: `wc -l plugins/dartai/skills/code-quality.md`
Expected: large (was ~244 lines). Record the number for the commit message.

- [ ] **Step 2: Verification — new line target**

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/code-quality.md`
Expected: `0`

- [ ] **Step 3: Replace the entire file content with this thin wrapper**

````markdown
---
name: code-quality
description: Thin dartai-specific wrapper that routes review-for-plan-updates proposals into Dart tasks. All code quality checklist content lives in .claude/rules/code-quality.md (project) and dev-standards:review-for-plan-updates (skill).
---

# Code Quality (dartai wrapper)

This skill is a **persistence wrapper only**. All review logic, trigger catalog, and proposal format live in `dev-standards:review-for-plan-updates`. Code quality standards (checklist, error handling, findability, cleanup) live in `.claude/rules/code-quality.md` — do not duplicate them here.

## What this skill does

1. Invokes `dev-standards:review-for-plan-updates` with the task diff
2. For each returned proposal:
   - Computes its fingerprint against `.claude/refactor-rejects.txt`
   - If not rejected, creates a Dart task in the `refactor-backlog` folder with tags `origin:review`, `parent:<task-id>`, `urgency:<low|medium|high>`
3. Returns a summary: number of proposals persisted, number skipped by reject list

## What this skill does NOT do

- Evaluate code quality directly — that content moved to `.claude/rules/code-quality.md` and the rule file is loaded automatically
- Run linters — use your language-specific lint command (see `.claude/rules/testing.md`)
- Decide whether to schedule a proposal now — that is the planner's decision at next planning cycle
- Edit code — never, under any circumstance

## Invocation

```
Called by:
  dartai:adversarial-quality-loop Phase 4.5
Returns:
  { persisted: <count>, rejected: <count>, total: <count> }
```

## Dart task format for persisted proposals

```yaml
dart_task:
  dartboard: "<same dartboard as surfacing task>"
  folder: "refactor-backlog"
  title: "<proposal.title>"
  description: |
    ## Origin
    Surfaced by review of task <surfacing-task-id> on <date>.

    ## Trigger
    <proposal.trigger>

    ## Evidence
    - Symbol: <proposal.evidence.symbol>
    - Observation: <proposal.evidence.observation>
    - Callers affected: <proposal.evidence.callers>

    ## Rationale
    <proposal.rationale>

    ## Estimated tier
    <proposal.estimated_tier>
  status: "To-do"
  priority: "Low | Medium | High"     # maps from urgency
  tags:
    - "origin:review"
    - "parent:<surfacing-task-id>"
    - "urgency:<low|medium|high>"
    - "principle:refactor-discipline.C"
```

## Reject list

`.claude/refactor-rejects.txt` is a newline-separated list of fingerprints. Each fingerprint is `<symbol>:<trigger>`. Before persisting a proposal, compute its fingerprint and skip if present. The planner appends to this file when it rejects a proposal.

## Related

- `dev-standards:review-for-plan-updates` — the reviewer this wrapper calls
- `dev-standards:grill-task` — when a proposal is later accepted, it gets grilled as a normal task
- `.claude/rules/code-quality.md` — project-specific code quality standards
- `.claude/rules/refactor-discipline.md` — A/B/C refactor rule
````

- [ ] **Step 4: Verify collapse**

Run: `wc -l plugins/dartai/skills/code-quality.md`
Expected: ≤ 80 lines (down from ~244).

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/code-quality.md`
Expected: ≥ 2

- [ ] **Step 5: Commit**

```bash
git add plugins/dartai/skills/code-quality.md
git commit -m "refactor(dartai): collapse code-quality skill to thin review wrapper"
```

---

## Task 13: Add rule-reading step to `dartai/skills/task-execution.md`

**Files:**
- Modify: `plugins/dartai/skills/task-execution.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'karpathy-principles\|refactor-discipline' plugins/dartai/skills/task-execution.md`
Expected: `0`

- [ ] **Step 2: Locate `### Step 0: Read Domain Model` (around line 42)**

Immediately **before** that step, insert a new Step -1:

```markdown
### Step -1: Read Project Rules

Before doing anything else, read the always-loaded rules in `.claude/rules/`:

- `karpathy-principles.md` — goal-driven execution, push back, verify
- `refactor-discipline.md` — A/B/C refactor rule
- `grill-intake.md` — task creation gate (already applied upstream, confirm here)
- `code-quality.md` — project-specific code quality standards
- `testing.md` — project-specific testing standards
- Any other `.md` files in `.claude/rules/`

These rules shape every decision the executor makes. They are thin pointers to skills — when a decision needs detail, invoke the referenced skill via `Skill` tool rather than guessing.

If `.claude/rules/` does not exist, warn the user: "Project has not run /dev-standards:setup-project. Task execution will proceed with defaults, but project-specific thresholds will not be respected." Do not block — proceed with sensible defaults.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'karpathy-principles\|refactor-discipline' plugins/dartai/skills/task-execution.md`
Expected: ≥ 2

Run: `grep -c '^### Step -1' plugins/dartai/skills/task-execution.md`
Expected: `1`

- [ ] **Step 4: Commit**

```bash
git add plugins/dartai/skills/task-execution.md
git commit -m "feat(dartai): read .claude/rules/ in task-execution Step -1"
```

---

## Task 14: Add rules precheck to `dartai/commands/start.md`

**Files:**
- Modify: `plugins/dartai/commands/start.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'karpathy-principles\|dev-standards:setup-project' plugins/dartai/commands/start.md`
Expected: `0`

- [ ] **Step 2: Locate `### 1.5 Check for Interrupted Loop` (around line 53)**

Insert a new section `### 1.6 Check for Project Rules` immediately after the 1.5 section. Place it **before** Section 2 (whatever that is — locate the next `^### ` line after the 1.5 block).

```markdown
### 1.6 Check for Project Rules

Before scheduling any work, verify the project has rules installed:

```bash
test -d .claude/rules && test -f .claude/rules/karpathy-principles.md
```

If the check fails, warn:

```
Project has not run /dev-standards:setup-project (or the last run predates the
grill integration). The loop will run with default thresholds. Run the setup
command for project-specific tuning.
```

Do NOT block. The loop can run on default thresholds — the warning is informational.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'karpathy-principles\|dev-standards:setup-project' plugins/dartai/commands/start.md`
Expected: ≥ 2

- [ ] **Step 4: Commit**

```bash
git add plugins/dartai/commands/start.md
git commit -m "feat(dartai): warn when .claude/rules/ missing on /dartai:start"
```

---

## Task 15: Add rule-reading preamble to `dartai/agents/task-executor.md`

**Files:**
- Modify: `plugins/dartai/agents/task-executor.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'karpathy-principles\|refactor-discipline' plugins/dartai/agents/task-executor.md`
Expected: `0`

- [ ] **Step 2: Locate `## Project-Specific Rules` section (around line 30)**

Add a line to that section's rule list. After the existing 4 rules (`autonomous-operation.md`, `eagle-eyed-discipline.md`, `execution-flow.md`, `phase-execution.md`), add:

```markdown
5. **`.claude/rules/karpathy-principles.md`** - Five named development principles (goal-driven, push back, verify, no scope creep)
6. **`.claude/rules/refactor-discipline.md`** - A/B/C refactor rule
```

Also, at the end of the `## Project-Specific Rules` section (right before `## Your Mission`), add:

```markdown
**Important**: `karpathy-principles.md` and `refactor-discipline.md` are thin reference files. When you need operational detail, invoke the skill they reference (e.g., `dev-standards:grill-task`, `dev-standards:refactor-first-assessment`, `dev-standards:review-for-plan-updates`) via the `Skill` tool. Do not try to act on rule content alone.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'karpathy-principles\|refactor-discipline' plugins/dartai/agents/task-executor.md`
Expected: ≥ 2

- [ ] **Step 4: Commit**

```bash
git add plugins/dartai/agents/task-executor.md
git commit -m "feat(dartai): task-executor agent reads karpathy and refactor rules"
```

---

# Phase D — workflow integration

Mirror the dartai wiring. Same invocation points, different persistence.

## Task 16: Wire grill-task into `workflow/commands/add-task.md`

**Files:**
- Modify: `plugins/workflow/commands/add-task.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'dev-standards:grill-task' plugins/workflow/commands/add-task.md`
Expected: `0`

- [ ] **Step 2: Locate `2. Interactive task creation` (around line 17)**

Replace that entire section with:

```markdown
2. **Invoke grill-task**

Call `dev-standards:grill-task` with the task title and any user-provided context. The skill probes project context, runs tier-gated interrogation, and returns a grilled `task_spec` and any `backflow_writes`.

- If grill returns `verdict: OK`, use the returned `task_spec` fields (priority → `task_spec.tier` mapping, description → `task_spec.requested`, acceptance → `task_spec.acceptance`, scope → `task_spec.scope`, context → `task_spec.refs`) in place of asking the user directly.
- If grill returns `verdict: TOO_LARGE_TO_GRILL`, do NOT write anything to `.workflow/tasks.md`. Report to the user that the task must be split, suggest a decomposition, and stop.
- If grill returns `verdict: ABORTED`, do nothing — the user cancelled during the confirmation screen.

Commit any `backflow_writes` to the project before proceeding to step 3.

Priority mapping from tier:
- `minimal` → Low
- `standard` → Medium
- `comprehensive` → Medium
- `architectural` → High
```

- [ ] **Step 3: Verify**

Run: `grep -c 'dev-standards:grill-task' plugins/workflow/commands/add-task.md`
Expected: ≥ 1

- [ ] **Step 4: Commit**

```bash
git add plugins/workflow/commands/add-task.md
git commit -m "feat(workflow): invoke dev-standards:grill-task in /workflow:add-task"
```

---

## Task 17: Add plan-update presentation to `workflow/skills/loop-orchestration.md`

At each loop tick, present pending plan updates from the previous review before dispatching the next task. Default "no" to preserve focus.

**Files:**
- Modify: `plugins/workflow/skills/loop-orchestration.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'pending_plan_updates\|plan-update presentation' plugins/workflow/skills/loop-orchestration.md`
Expected: `0`

- [ ] **Step 2: Locate the "Subagent Lifecycle Management" section (around line 56-80)**

After the `lifecycle` block ends, add a new section:

```markdown
## Plan-Update Presentation Between Ticks

Between each tick (after a subagent returns, before the next spawn), read `.workflow/loop-state.json` for any `pending_plan_updates` written by the last task's review step.

```yaml
tick_transition:
  read: ".workflow/loop-state.json"
  extract: "pending_plan_updates[]"
  if_any:
    present_to_user: |
      The last task surfaced <N> plan-update proposals:

      <for each proposal>
        <title> (<trigger>, urgency:<urgency>)
      </for>

      Schedule any of these now? (default: no, they stay in backlog)
    default_answer: "no"
    on_accept:
      action: "Invoke dev-standards:grill-task on the accepted proposal, insert as next task"
    on_defer:
      action: "Leave in pending_plan_updates with urgency tag"
    on_reject:
      action: "Remove from pending_plan_updates, append fingerprint to .claude/refactor-rejects.txt"

never:
  - "Auto-accept proposals"
  - "Block next tick on proposal decisions"
```

The goal is to surface the backlog without interrupting focus. Default "no" keeps the loop moving.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'pending_plan_updates' plugins/workflow/skills/loop-orchestration.md`
Expected: ≥ 2

Run: `grep -c '## Plan-Update Presentation' plugins/workflow/skills/loop-orchestration.md`
Expected: `1`

- [ ] **Step 4: Commit**

```bash
git add plugins/workflow/skills/loop-orchestration.md
git commit -m "feat(workflow): present pending plan-updates between loop ticks"
```

---

## Task 18: Collapse `workflow/skills/adversarial-quality.md` into thin wrapper

Same pattern as Task 12 — the quality-loop skill becomes a thin wrapper calling `dev-standards:review-for-plan-updates` for the review step. Other phases stay, but the review-specific content gets removed.

**Files:**
- Modify: `plugins/workflow/skills/adversarial-quality.md`

- [ ] **Step 1: Verification — record current line count**

Run: `wc -l plugins/workflow/skills/adversarial-quality.md`
Expected: large (~524 lines). Record this.

- [ ] **Step 2: Verification — new reference not yet present**

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/workflow/skills/adversarial-quality.md`
Expected: `0`

- [ ] **Step 3: Insert Phase 4.5 before Phase 5**

Locate `### Phase 5: Quality Gates` (around line 304) and immediately before it (after Phase 4's closing checkpoint text around line 302), insert:

```markdown
### Phase 4.5: Review for Plan Updates (C-class refactor discovery)

**Objective**: Emit C-class refactor findings as plan-update proposals without editing code.

**Pattern**: Invoke `dev-standards:review-for-plan-updates`, persist proposals into `.workflow/loop-state.json`.

```yaml
invoke:
  tool: Skill
  skill: "dev-standards:review-for-plan-updates"
  input:
    diff: "git diff <task-base>..HEAD"
    rules_dir: ".claude/rules/"

persist:
  file: ".workflow/loop-state.json"
  field: "pending_plan_updates"
  append: "<proposals returned, filtered against .claude/refactor-rejects.txt>"

do_not:
  - "Edit code based on findings"
  - "Decide accept/defer/reject here"
  - "Expand trigger catalog beyond what the skill defines"
```

**Checkpoint**: Proposals written to state file. The next tick's plan-update presentation (see `loop-orchestration.md`) handles user interaction.

---
```

- [ ] **Step 4: Confirm no edits are needed beyond the Phase 4.5 addition**

Phase 4.5 is **purely additive**. The existing workflow quality-loop content (TDD setup, implementation, self-review, concurrent verification, quality gates, post-task review, final validation) stays unchanged. Do NOT remove or rewrite any existing sections in this file. The trigger catalog duplication concern is handled structurally: the dev-standards skill now owns the trigger catalog, and the workflow adversarial-quality loop invokes it by reference in Phase 4.5. Any existing inline language in the workflow file that happens to use similar words (e.g. "parameter" or "complexity") is fine to leave alone — it is prose context, not a duplicated trigger catalog.

Run: `grep -n 'parameter bloat\|visible complexity debt' plugins/workflow/skills/adversarial-quality.md`
Expected: zero or a small number. No action required either way. This is an observability check, not an edit trigger.

- [ ] **Step 5: Verify**

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/workflow/skills/adversarial-quality.md`
Expected: ≥ 1

Run: `grep -c '^### Phase 4.5' plugins/workflow/skills/adversarial-quality.md`
Expected: `1`

- [ ] **Step 6: Commit**

```bash
git add plugins/workflow/skills/adversarial-quality.md
git commit -m "feat(workflow): add Phase 4.5 review-for-plan-updates in quality loop"
```

---

## Task 19: Add rules precheck to `workflow/commands/start-loop.md`

**Files:**
- Modify: `plugins/workflow/commands/start-loop.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'karpathy-principles\|dev-standards:setup-project' plugins/workflow/commands/start-loop.md`
Expected: `0`

- [ ] **Step 2: Locate `### 0. Check for Interrupted Loop` (around line 40-58)**

Insert a new section `### 0.5 Check for Project Rules` immediately after section 0. The content is the same warning used in dartai Task 14, adjusted for workflow:

```markdown
### 0.5 Check for Project Rules

Before starting, verify the project has rules installed:

```bash
test -d .claude/rules && test -f .claude/rules/karpathy-principles.md
```

If the check fails, warn:

```
Project has not run /dev-standards:setup-project (or the last run predates the
grill integration). The loop will run with default thresholds. Run the setup
command for project-specific tuning.
```

Do NOT block. The loop can run on default thresholds — the warning is informational.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'karpathy-principles\|dev-standards:setup-project' plugins/workflow/commands/start-loop.md`
Expected: ≥ 2

- [ ] **Step 4: Commit**

```bash
git add plugins/workflow/commands/start-loop.md
git commit -m "feat(workflow): warn when .claude/rules/ missing on /workflow:start-loop"
```

---

## Task 20: Add rule-reading preamble to `workflow/agents/task-executor.md`

**Files:**
- Modify: `plugins/workflow/agents/task-executor.md`

- [ ] **Step 1: Verification currently fails**

Run: `grep -c 'karpathy-principles\|refactor-discipline' plugins/workflow/agents/task-executor.md`
Expected: `0`

- [ ] **Step 2: Locate the agent preamble (find the `## Project-Specific Rules` section or equivalent at the top of the agent file)**

Run: `grep -n '^## \|^# ' plugins/workflow/agents/task-executor.md | head -20` to find section headings.

Find the first section that describes what rules the agent reads (likely near the top). Add:

```markdown
## Always-Loaded Project Rules

Before executing any task, read `.claude/rules/karpathy-principles.md` and `.claude/rules/refactor-discipline.md` in the project root. These are thin reference files — when you need operational detail, invoke the skill they reference via the `Skill` tool.

The rules point to:
- `dev-standards:grill-task` — task intake interrogation
- `dev-standards:refactor-first-assessment` — A-rule preparatory refactor check
- `dev-standards:review-for-plan-updates` — C-rule post-task review

If `.claude/rules/` is missing or the karpathy file is absent, proceed with defaults and report the absence to the loop orchestrator.
```

Place this after any existing "CRITICAL" or rule-reading preamble that already exists in the file, or at the top of the agent body if none exists.

- [ ] **Step 3: Verify**

Run: `grep -c 'karpathy-principles\|refactor-discipline' plugins/workflow/agents/task-executor.md`
Expected: ≥ 2

- [ ] **Step 4: Commit**

```bash
git add plugins/workflow/agents/task-executor.md
git commit -m "feat(workflow): task-executor agent reads karpathy and refactor rules"
```

---

# Phase E — End-to-end verification

## Task 21: Rule file line-count and context-rot check

**Files:** (read-only)
- `plugins/dev-standards/assets/templates/rules/karpathy-principles.md`
- `plugins/dev-standards/assets/templates/rules/refactor-discipline.md`
- `plugins/dev-standards/assets/templates/rules/grill-intake.md`

- [ ] **Step 1: Run line-count verification**

Run:

```bash
for f in plugins/dev-standards/assets/templates/rules/karpathy-principles.md \
         plugins/dev-standards/assets/templates/rules/refactor-discipline.md \
         plugins/dev-standards/assets/templates/rules/grill-intake.md; do
  lines=$(wc -l < "$f")
  echo "$f: $lines lines"
done
```

Expected: each file ≤ 60 lines.

- [ ] **Step 2: Verify no embedded procedures (rules are pointers)**

Run:

```bash
for f in plugins/dev-standards/assets/templates/rules/karpathy-principles.md \
         plugins/dev-standards/assets/templates/rules/refactor-discipline.md \
         plugins/dev-standards/assets/templates/rules/grill-intake.md; do
  steps=$(grep -cE '^(### )?Step [0-9]|^1\. |^2\. |^3\. ' "$f" || true)
  echo "$f: $steps numbered steps (should be low — rules are pointers, not procedures)"
done
```

Expected: each file has ≤ 2 numbered steps.

- [ ] **Step 3: Verify spec's 150-line always-loaded target**

Run:

```bash
sum=0
for f in plugins/dev-standards/assets/templates/rules/karpathy-principles.md \
         plugins/dev-standards/assets/templates/rules/refactor-discipline.md \
         plugins/dev-standards/assets/templates/rules/grill-intake.md; do
  sum=$((sum + $(wc -l < "$f")))
done
echo "Total always-loaded rule lines (new files): $sum"
```

Expected: total ≤ 180 (spec target was ~150; 20 line buffer for frontmatter/titles is fine).

If total exceeds 180, return to the relevant rule file and trim. Do not proceed until under the threshold.

- [ ] **Step 4: No commit needed — this task is verification-only**

If any check fails, fix the offending file and re-run this task's checks before proceeding.

---

## Task 22: Collapsed-skill line-count verification

**Files:** (read-only)
- `plugins/dartai/skills/code-quality.md`
- `plugins/workflow/skills/adversarial-quality.md`

- [ ] **Step 1: Verify dartai code-quality collapse**

Run: `wc -l plugins/dartai/skills/code-quality.md`
Expected: ≤ 80 lines (spec target was < 80). Was ~244 before.

- [ ] **Step 2: Verify workflow adversarial-quality includes Phase 4.5**

Run: `grep -c '^### Phase 4.5' plugins/workflow/skills/adversarial-quality.md`
Expected: `1`

Note: workflow's `adversarial-quality.md` is not expected to shrink — Phase 4.5 is purely additive per Task 18. The file retains its TDD/quality-gate content unchanged. The dev-standards skill now owns the C-class trigger catalog, and Phase 4.5 invokes it by reference — no inline removal required.

- [ ] **Step 3: Verify dartai code-quality references the dev-standards skill**

Run: `grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/code-quality.md`
Expected: ≥ 2

- [ ] **Step 4: No commit — verification only**

---

## Task 23: Integration points invocation audit

Verify every integration point in the spec actually has the invocation added.

**Files:** (read-only)
- All files modified in Phases C and D

- [ ] **Step 1: Dartai integration audit**

Run:

```bash
echo "dartai/commands/task.md:             $(grep -c 'dev-standards:grill-task' plugins/dartai/commands/task.md) grill-task refs"
echo "dartai/skills/simple-planning.md:    $(grep -c 'dev-standards:grill-task' plugins/dartai/skills/simple-planning.md) grill-task refs, $(grep -c 'dev-standards:refactor-first-assessment' plugins/dartai/skills/simple-planning.md) refactor-first refs"
echo "dartai/skills/adversarial-planning-loop.md: $(grep -c 'dev-standards:grill-task' plugins/dartai/skills/adversarial-planning-loop.md) grill-task refs"
echo "dartai/skills/adversarial-quality-loop.md:  $(grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/adversarial-quality-loop.md) review refs"
echo "dartai/skills/code-quality.md:       $(grep -c 'dev-standards:review-for-plan-updates' plugins/dartai/skills/code-quality.md) review refs"
echo "dartai/skills/task-execution.md:     $(grep -c 'karpathy-principles' plugins/dartai/skills/task-execution.md) karpathy refs"
echo "dartai/commands/start.md:            $(grep -c 'karpathy-principles\|setup-project' plugins/dartai/commands/start.md) rule-check refs"
echo "dartai/agents/task-executor.md:      $(grep -c 'karpathy-principles' plugins/dartai/agents/task-executor.md) karpathy refs"
```

Expected: every line shows ≥ 1 for its target reference.

- [ ] **Step 2: Workflow integration audit**

Run:

```bash
echo "workflow/commands/add-task.md:           $(grep -c 'dev-standards:grill-task' plugins/workflow/commands/add-task.md) grill-task refs"
echo "workflow/skills/loop-orchestration.md:   $(grep -c 'pending_plan_updates' plugins/workflow/skills/loop-orchestration.md) plan-update refs"
echo "workflow/skills/adversarial-quality.md:  $(grep -c 'dev-standards:review-for-plan-updates' plugins/workflow/skills/adversarial-quality.md) review refs"
echo "workflow/commands/start-loop.md:         $(grep -c 'karpathy-principles\|setup-project' plugins/workflow/commands/start-loop.md) rule-check refs"
echo "workflow/agents/task-executor.md:        $(grep -c 'karpathy-principles' plugins/workflow/agents/task-executor.md) karpathy refs"
```

Expected: every line shows ≥ 1 for its target reference.

- [ ] **Step 3: Setup-project copy audit**

Run:

```bash
grep -c 'karpathy-principles.md\|refactor-discipline.md\|grill-intake.md' plugins/dev-standards/commands/setup-project.md
```

Expected: `3` (one reference per rule template in the Step 2 copy block).

- [ ] **Step 4: Skill frontmatter sanity**

Run:

```bash
for f in plugins/dev-standards/skills/grill-task/SKILL.md \
         plugins/dev-standards/skills/review-for-plan-updates/SKILL.md \
         plugins/dev-standards/skills/refactor-first-assessment/SKILL.md; do
  echo "=== $f ==="
  head -5 "$f"
done
```

Expected: each file opens with `---`, has `name:` matching directory name, has `description:`, closes with `---`.

- [ ] **Step 5: No commit — verification only**

If any audit fails, return to the offending task and fix.

---

## Task 24: Manual end-to-end smoke test

Exercise the new wiring against a scratch project to catch any misses in the plan.

**Files:**
- Create: `/tmp/grill-smoke/` (scratch project)

- [ ] **Step 1: Create scratch project**

```bash
rm -rf /tmp/grill-smoke
mkdir -p /tmp/grill-smoke/src
cd /tmp/grill-smoke
git init -q
echo "# Smoke Project" > README.md
echo 'console.log("hello");' > src/index.js
cat > package.json <<'EOF'
{ "name": "grill-smoke", "version": "0.0.0", "scripts": { "test": "echo no tests yet" } }
EOF
git add -A && git commit -qm "initial"
```

- [ ] **Step 2: Run setup-project via Claude Code**

In Claude Code session, with `/tmp/grill-smoke/` as cwd, run: `/dev-standards:setup-project`

Answer the interview with: web app, CRUD, local-only, standard TDD, standard docs.

- [ ] **Step 3: Verify rules installed**

Run:

```bash
ls /tmp/grill-smoke/.claude/rules/
```

Expected: lists `version-control.md`, `code-quality.md`, `architecture.md`, `karpathy-principles.md`, `refactor-discipline.md`, `grill-intake.md`, plus any language/testing rules.

Run: `test -f /tmp/grill-smoke/.claude/rules/karpathy-principles.md && echo OK`
Expected: `OK`

- [ ] **Step 4: Trigger grill-task via workflow add-task**

In the same Claude Code session, run: `/workflow:add-task "Add input validation to the index.js hello output"`

Expected: grill-task runs, asks about Intent/Scope/Verification (docs are standard so no user-story layer), proposes a task spec, asks for confirmation.

Abort at the confirmation screen to avoid side effects.

- [ ] **Step 5: Verify grill-task emitted backflow writes correctly or degraded gracefully**

Check: did the grill probe for project-local writers and fall back to TODO lines if absent? The standard docs level installs `write-tech-spec` but not `write-user-story`, so the Intent layer should either skip (if no intent gap) or record a TODO.

If the grill errors trying to invoke a missing skill, that is a bug in Task 6's graceful degradation implementation — fix before proceeding.

- [ ] **Step 6: Trigger review-for-plan-updates manually**

Create a minimal "completed" state in the smoke project:

```bash
cd /tmp/grill-smoke
cat > src/index.js <<'EOF'
function greet(a, b, c, d, e, f, g, h, i) {
  return `${a} ${b} ${c} ${d} ${e} ${f} ${g} ${h} ${i}`;
}
console.log(greet("h","e","l","l","o","w","o","r","d"));
EOF
git add -A && git commit -qm "implement greet"
```

In Claude Code, invoke: `Skill dev-standards:review-for-plan-updates` with the prompt "Review HEAD~1..HEAD in /tmp/grill-smoke".

Expected: the skill surfaces a `parameter_bloat` proposal for `greet`. Proposal should be structured YAML, reference `src/index.js:greet`, carry urgency `low` or `medium`, and not modify any code.

- [ ] **Step 7: Verify no code was edited by the review**

Run:

```bash
cd /tmp/grill-smoke
git diff HEAD
```

Expected: empty. Review should never edit code.

- [ ] **Step 8: Cleanup smoke project**

```bash
rm -rf /tmp/grill-smoke
```

- [ ] **Step 9: No commit — this task is verification only**

If any smoke-test step fails, return to the relevant phase's tasks and fix.

---

## Task 25: Final commit audit and merge readiness

- [ ] **Step 1: Review commit history**

Run: `git log --oneline feat/dev-standards-grill-integration ^main`
Expected: one commit per task (Tasks 0-20 are commit-producing; Tasks 21-24 are verification-only). Count should be ~21 commits.

- [ ] **Step 2: Verify no uncommitted changes**

Run: `git status -s`
Expected: empty.

- [ ] **Step 3: Verify no debug or TODO residue**

Run:

```bash
grep -rn 'TODO\|FIXME\|XXX\|HACK' plugins/dev-standards/skills/grill-task \
  plugins/dev-standards/skills/review-for-plan-updates \
  plugins/dev-standards/skills/refactor-first-assessment \
  plugins/dev-standards/assets/templates/rules/karpathy-principles.md \
  plugins/dev-standards/assets/templates/rules/refactor-discipline.md \
  plugins/dev-standards/assets/templates/rules/grill-intake.md
```

Expected: only intentional `TODO: install` strings from the graceful-degradation explanation in grill-task's SKILL.md. No unintentional TODO/FIXME.

- [ ] **Step 4: Verify plugin validation still passes**

Run: `claude plugin validate plugins/dev-standards` (and similarly for `plugins/dartai`, `plugins/workflow`)
Expected: all three validate cleanly.

If `claude plugin validate` is unavailable in the environment, do a structural check:

```bash
for p in dev-standards dartai workflow; do
  test -f plugins/$p/.claude-plugin/plugin.json && echo "$p: plugin.json OK" || echo "$p: MISSING plugin.json"
done
```

Expected: three `OK` lines.

- [ ] **Step 5: Merge-ready signal**

All phases done. The worktree branch `feat/dev-standards-grill-integration` is ready to merge back to `main` via PR. Do NOT merge from this plan — leave the final merge to the user.

- [ ] **Step 6: No commit — this task is verification only**

---

# Completion checklist

When every task above is done:

- [ ] Phase A: 3 new rule template files exist, each ≤ 60 lines, total ≤ 180 lines
- [ ] Phase A: `setup-project.md` Phase 5 Step 2 copies all three new rules
- [ ] Phase B: 3 new dev-standards skills exist with valid frontmatter
- [ ] Phase B: `grill-task` documents 6 question layers and graceful degradation
- [ ] Phase B: `review-for-plan-updates` documents 7 triggers and never edits code
- [ ] Phase C: dartai has grill invocations in `commands/task.md`, `skills/simple-planning.md`, `skills/adversarial-planning-loop.md`
- [ ] Phase C: dartai has review invocations in `skills/adversarial-quality-loop.md`, `skills/code-quality.md`
- [ ] Phase C: `dartai/skills/code-quality.md` collapsed to ≤ 80 lines
- [ ] Phase C: dartai task-executor agent and task-execution skill read the new rules
- [ ] Phase D: workflow has grill invocation in `commands/add-task.md`
- [ ] Phase D: workflow has plan-update presentation in `skills/loop-orchestration.md`
- [ ] Phase D: workflow has review invocation in `skills/adversarial-quality.md` (Phase 4.5 added)
- [ ] Phase D: workflow task-executor agent reads the new rules
- [ ] Phase E: all verification tasks pass
- [ ] Phase E: smoke test against scratch project succeeds

---

# Out-of-scope (will not be done in this plan)

These are explicitly not this plan's work, per the spec's Non-Goals:

- Creating `decide`, `update-rules`, `add-skill`, `update-project` skills — already exist
- Creating `write-user-story`, `define-user-flow`, `domain-*` skill templates — already exist
- Changing `mcp.json.disabled` configuration — intentional and off-limits
- Replacing any `superpowers:*` skill — this integration composes with them
- Enforcing opportunistic (B-class) refactoring — explicitly forbidden
- Implementing a new task store — dartai keeps Dart, workflow keeps loop-state.json
- Adding CI to test the markdown edits — markdown plugins are verified by structural grep, not unit tests
