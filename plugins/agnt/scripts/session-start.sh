#!/bin/bash
# agnt SessionStart hook
#
# NOTE: Auto-start of scripts and proxies from .agnt.kdl is now handled
# server-side by the daemon during SESSION REGISTER. This hook is kept
# for potential future session initialization needs.

exit 0
