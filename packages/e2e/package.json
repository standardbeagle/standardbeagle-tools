{
  "name": "@standardbeagle/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "smoke": "vitest run --config vitest.config.ts",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "eslint": "^10.2.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
